name: Проверка данных сайта
on:
  push:
    paths: ['data/**', 'assets/images/**', 'tools/validate_data.py']
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tools/validate_data.py
